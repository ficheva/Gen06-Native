<Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:header="utils/action-bar" loaded="pageLoaded">

    <GridLayout rows="auto, *">
        <header:action-bar />
        <StackLayout row="1">

            <StackLayout cssClass="form-view">
                <GridLayout columns="*, *" rows="auto">
                    <StackLayout height="50" />
                    <Label text="Username" cssClass="label" />
                    <TextField col="1" text="{{ username }}" cssClass="input" />
                </GridLayout>
                <StackLayout cssClass="form-item-border" height="1"></StackLayout>

                <GridLayout columns="*, *" rows="auto">
                    <StackLayout height="50" />
                    <Label text="Password" cssClass="label" />
                    <TextField col="1" text="{{ password }}" secure="true" cssClass="input" />
                </GridLayout>
                <StackLayout cssClass="form-item-border" height="1"></StackLayout>

                <GridLayout columns="*" rows="auto" padding="10">
                    <Button text="Sign In" tap="{{ onLogin }}" />
                </GridLayout>

                <Button text="Register" tap="{{ onShowRegister }}" />

            </StackLayout>

            <!-- additional view content -->
        </StackLayout>
    </GridLayout>
</Page>